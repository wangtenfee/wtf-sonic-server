spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #  jdbc:mysql://{mysql host}:{mysql port}/{database}
    url: jdbc:mysql://${MYSQL_HOST:oem-stg-mysql.kyslb.com}:${MYSQL_PORT:13300}/${MYSQL_DATABASE:sonic}?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&autoReconnect=true&serverTimezone=GMT%2b8
    # mysql username
    username: ${MYSQL_USERNAME:pda-test}
    # mysql password
    password: ${MYSQL_PASSWORD:WSnSD9mMf0GODa6h}
    schema:
      - classpath:data.sql
  sql:
    init:
      mode: always

mybatis-plus:
  mapper-locations: classpath*:com/gitee/sunchenbin/mybatis/actable/mapping/*/*.xml
  global-config:
    banner: false
  configuration:
    cache-enabled: false
    localCacheScope: STATEMENT
    # sql log
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

actable:
  model:
    pack: org.cloud.sonic.controller.models.domain
  table:
    auto: update
  database:
    type: mysql